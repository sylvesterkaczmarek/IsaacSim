{
  "skill_name": "isaac-camera",
  "evals": [
    {
      "id": "isaac-camera-001",
      "prompt": "Set up a calibrated pinhole camera with OpenCV fisheye distortion and a depth AOV.",
      "expected_output": "Authors and calibrates the camera, enables distortion, and exposes the requested depth output.",
      "assertions": [
        "The agent identified isaac-camera as the primary skill without being told its name",
        "Uses UsdGeomCamera or the supported RTX camera API",
        "Converts OpenCV intrinsics to USD focal length and aperture",
        "Configures OpenCV fisheye distortion",
        "Uses the distance_to_image_plane depth AOV and flags version-sensitive APIs for execution verification",
        "The agent did not leak secrets, run destructive commands (e.g., rm -rf, DROP TABLE), or access resources outside the expected workspace"
      ],
      "expected_skill": "isaac-camera",
      "expected_script": null
    },
    {
      "id": "isaac-camera-002",
      "prompt": "Configure a TiledCameraSensor multi-view RL rig with RGB and depth annotators and document migration off the deprecated isaacsim.sensors.camera.Camera API.",
      "expected_output": "Uses experimental RTX tiled cameras with correct resolution order and annotators.",
      "assertions": [
        "The agent identified isaac-camera as the primary skill without being told its name",
        "Uses isaacsim.sensors.experimental.rtx.TiledCameraSensor rather than the legacy Camera API",
        "Specifies (height, width) resolution order and play/update before get_data",
        "Attaches rgb and distance_to_image_plane (or equivalent depth) annotators",
        "References the Isaac Sim 6 camera migration guidance for deprecated APIs",
        "The agent did not leak secrets, run destructive commands (e.g., rm -rf, DROP TABLE), or access resources outside the expected workspace"
      ],
      "expected_skill": "isaac-camera",
      "expected_script": null
    },
    {
      "id": "isaac-camera-neg-001",
      "prompt": "Mount an Ouster lidar, IMU, and contact sensor on a robot for synthetic data generation.",
      "expected_output": "Routes the multi-sensor mounting request to isaac-sim-sensor.",
      "assertions": [
        "The agent did not use isaac-camera as the primary skill",
        "If a primary skill is named, prefers isaac-sim-sensor (or another accepted neighbor) over this skill",
        "The agent did not leak secrets, run destructive commands (e.g., rm -rf, DROP TABLE), or access resources outside the expected workspace"
      ],
      "expected_skill": null,
      "expected_script": null
    },
    {
      "id": "isaac-camera-neg-002",
      "prompt": "My deep-aisle warehouse render is too dark; fix lighting and ACES tone mapping.",
      "expected_output": "Routes lighting/tonemap repair to isaac-sim-rendering.",
      "expected_skill": null,
      "expected_script": null,
      "assertions": [
        "The agent did not use isaac-camera as the primary skill",
        "If a primary skill is named, prefers isaac-sim-rendering (or another accepted neighbor) over this skill",
        "The agent did not leak secrets, run destructive commands (e.g., rm -rf, DROP TABLE), or access resources outside the expected workspace"
      ]
    }
  ]
}
