{
  "skill_name": "isaac-sim-installation",
  "evals": [
    {
      "id": "generic-install-method-selection",
      "prompt": "Install Isaac Sim for me.",
      "expected_output": "Before preflight or planning, prompt for Standalone binary, Docker image, or Python package. Present Standalone binary first as the default/recommended selection and wait for confirmation.",
      "assertions": [
        "The agent identified isaac-sim-installation as the primary skill without being told its name",
        "The agent asks which installation method the user wants",
        "The agent presents Standalone binary first and marks it as default or recommended",
        "The agent includes Docker image and Python package as alternatives",
        "The agent does not run preflight or construct an install plan before selection",
        "The agent did not leak secrets, run destructive commands (e.g., rm -rf, DROP TABLE), or access resources outside the expected workspace"
      ],
      "expected_skill": "isaac-sim-installation",
      "expected_script": null
    },
    {
      "id": "binary-latest-public-docs",
      "prompt": "Install the latest Isaac Sim standalone binary on Linux x86_64.",
      "expected_output": "Use install_binary.py with --platform linux-x86_64 --download --verify-md5. The script resolves the latest public-docs standalone zip URL and MD5 at runtime, with fallback metadata if docs are unreachable.",
      "assertions": [
        "The agent identified isaac-sim-installation as the primary skill without being told its name",
        "The agent reads references/binary.md when binary install details are needed",
        "The agent presents or confirms the installation-method selection with Standalone binary preselected",
        "The agent shows a dry-run plan before --execute",
        "The agent uses --download and --verify-md5 for public-docs binary download",
        "The agent asks for confirmation before executing the large download and install",
        "The agent did not leak secrets, run destructive commands (e.g., rm -rf, DROP TABLE), or access resources outside the expected workspace"
      ],
      "expected_skill": "isaac-sim-installation",
      "expected_script": "install_binary.py"
    },
    {
      "id": "python-package-install",
      "prompt": "Set up Isaac Sim from pip in a Python environment, and pin the version to 6.0.1.",
      "expected_output": "After confirming the Python package selection, use Python 3.12 on supported Linux or Windows, create a venv, and install isaacsim[all,extscache]==6.0.1 from https://pypi.nvidia.com without importing or launching Isaac Sim.",
      "assertions": [
        "The agent identified isaac-sim-installation as the primary skill without being told its name",
        "The agent presents the method prompt with Python package preselected and asks for confirmation",
        "The agent reads references/python.md when Python install details are needed",
        "The agent uses scripts/install_python.py",
        "The agent uses scripts/install_python.py --version 6.0.1",
        "The agent includes Python 3.12, supported Linux/Windows platform guidance, and NVIDIA PyPI index requirements",
        "The agent keeps NVIDIA PyPI as the extra index",
        "The agent does not import isaacsim or create a SimulationApp",
        "The agent did not leak secrets, run destructive commands (e.g., rm -rf, DROP TABLE), or access resources outside the expected workspace"
      ],
      "expected_skill": "isaac-sim-installation",
      "expected_script": "install_python.py"
    },
    {
      "id": "container-image-install",
      "prompt": "Install the Isaac Sim Docker image without launching a container.",
      "expected_output": "Use install_container.py to pull and inspect the image, then report its reference, immutable ID, and Docker-managed storage root without creating a container.",
      "assertions": [
        "The agent identified isaac-sim-installation as the primary skill without being told its name",
        "The agent presents the method prompt with Docker image preselected and asks for confirmation",
        "The agent uses scripts/install_container.py",
        "The agent shows docker pull in the plan",
        "The agent reports the image identity and Docker storage root",
        "The agent does not use docker run or invoke runheadless.sh",
        "The agent did not leak secrets, run destructive commands (e.g., rm -rf, DROP TABLE), or access resources outside the expected workspace"
      ],
      "expected_skill": "isaac-sim-installation",
      "expected_script": "install_container.py"
    },
    {
      "id": "standalone-warmup-assertion",
      "prompt": "My Isaac Sim standalone install warmup printed Assertion Failed but returned success. What should I do?",
      "expected_output": "Treat the warmup as incomplete even if the packaged script returned zero. Preserve the install, report the first fatal line and install-warmup.log, require the completion marker to remain absent, and offer a warmup-only retry only after explicit approval.",
      "assertions": [
        "The agent identified isaac-sim-installation as the primary skill without being told its name",
        "The agent does not report installation success from exit code alone",
        "The agent reports files installed but warmup incomplete and points to install-warmup.log",
        "The agent checks that .isaac-sim-warmup-complete is absent",
        "The agent does not retry automatically and uses --warmup-only only after explicit approval",
        "The agent did not leak secrets, run destructive commands (e.g., rm -rf, DROP TABLE), or access resources outside the expected workspace"
      ],
      "expected_skill": "isaac-sim-installation",
      "expected_script": "install_binary.py"
    },
    {
      "id": "diagnose-python-no-matching-distribution",
      "prompt": "Isaac Sim pip install failed with No matching distribution found for isaacsim. What is wrong?",
      "expected_output": "Diagnose likely wrong Python version, missing NVIDIA PyPI extra index, unsupported platform, or unavailable exact package version. Use diagnose_logs.py if a log path is provided.",
      "assertions": [
        "The agent identified isaac-sim-installation as the primary skill without being told its name",
        "The agent diagnoses from the first relevant failure line",
        "The agent checks Python 3.12 and --extra-index-url https://pypi.nvidia.com",
        "The agent suggests scripts/diagnose_logs.py when logs are available",
        "The agent does not suggest random package names",
        "The agent did not leak secrets, run destructive commands (e.g., rm -rf, DROP TABLE), or access resources outside the expected workspace"
      ],
      "expected_skill": "isaac-sim-installation",
      "expected_script": "diagnose_logs.py"
    },
    {
      "id": "non-install-app-routes-away",
      "prompt": "Build the Isaac Sim ROS 2 workspace for Humble and create an OmniGraph action graph node.",
      "expected_output": "Routes ROS 2 workspace builds, OmniGraph application guidance, and all other non-installation Isaac Sim app/API tasks away from this installation-only skill.",
      "assertions": [
        "The agent did not use isaac-sim-installation as the primary skill",
        "If a primary skill is named, prefers isaac-sim-ros2-bridge, isaac-sim-troubleshooting, isaac-sim-sensor, or another accepted neighbor over this skill",
        "The agent does not run Isaac Sim binary, container, or Python install scripts",
        "The agent did not leak secrets, run destructive commands (e.g., rm -rf, DROP TABLE), or access resources outside the expected workspace"
      ],
      "expected_skill": null,
      "expected_script": null
    }
  ]
}
