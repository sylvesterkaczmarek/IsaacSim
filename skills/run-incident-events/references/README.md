# References — run-incident-events

Loaded on demand. Read `SKILL.md` first.

| File | Read it when |
|---|---|
| [`api-reference.md`](api-reference.md) | You need exact signatures, tag attribute names and vocabularies, semantic labels, or the manager lifecycle. |
| [`runtime-notes.md`](runtime-notes.md) | An event silently did not happen, or you are parsing `incidents_report.json`. |

Everything here was read from the `isaacsim.replicator.incident.core` source as
resolved by the Action and Event Data Generation app. It is consumed from
`extscache`; it is authored in the `metrosim` repo, and the app's `.kit` file is
where its exact version lives.
