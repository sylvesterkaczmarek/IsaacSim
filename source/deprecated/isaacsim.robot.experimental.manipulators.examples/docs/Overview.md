# Overview

This extension is deprecated. It contains import shims only. Use `isaacsim.robot_motion.examples`.

Equivalent imports are forwarded with a warning. Removed APIs raise an error with a replacement path.
