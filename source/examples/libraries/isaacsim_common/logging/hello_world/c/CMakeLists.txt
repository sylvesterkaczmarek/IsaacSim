# SPDX-FileCopyrightText: Copyright (c) 2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0

if(NOT DEFINED ISAACSIM_CMAKE_MINIMUM_VERSION OR NOT DEFINED ISAACSIM_CMAKE_POLICY_MAXIMUM_VERSION)
    message(FATAL_ERROR "Configure this example through source/examples/example.sh or example.bat")
endif()
cmake_minimum_required(VERSION ${ISAACSIM_CMAKE_MINIMUM_VERSION}...${ISAACSIM_CMAKE_POLICY_MAXIMUM_VERSION})
project(IsaacSimHelloWorldC LANGUAGES C)

if(NOT DEFINED ISAACSIM_C_STANDARD)
    message(FATAL_ERROR "ISAACSIM_C_STANDARD was not provided by the example wrapper")
endif()

find_package(isaacsim_common CONFIG REQUIRED)

add_executable(hello_world_c Main.c)
set_target_properties(hello_world_c PROPERTIES
    C_STANDARD "${ISAACSIM_C_STANDARD}"
    C_STANDARD_REQUIRED ON
    C_EXTENSIONS OFF
    RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin"
)
foreach(configuration DEBUG RELEASE RELWITHDEBINFO MINSIZEREL)
    set_target_properties(hello_world_c PROPERTIES
        "RUNTIME_OUTPUT_DIRECTORY_${configuration}" "${CMAKE_BINARY_DIR}/bin"
    )
endforeach()
target_link_libraries(hello_world_c PRIVATE isaacsim::common-logging)
