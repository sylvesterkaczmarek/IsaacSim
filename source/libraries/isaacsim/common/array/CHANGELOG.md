# Changelog

## [Unreleased]

### Added
- Add `Array`, `DType`, `Shape`, `Device`, and `DeviceGuard` types for multi-dimensional array operations.

### Fixed
- Support loading the CUDA runtime on Windows.
