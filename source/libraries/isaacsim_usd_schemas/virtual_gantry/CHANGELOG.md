# Changelog

## [Unreleased]

### Added

- `IsaacVirtualGantry`: an `Xform`-derived typed prim whose world transform is the anchor of a one-sided spring-damper rope, with the `isaac:gantry:*` attributes and the `attachBody` / `articulation` relationships. Built, generated, tested and packaged through the standalone library CMake build.
